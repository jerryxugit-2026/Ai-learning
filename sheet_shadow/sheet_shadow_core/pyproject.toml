[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "sheet_shadow_core"
version = "0.1.0"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["openpyxl>=3.1,<4"]
